{
	"$schema": "https://opencode.ai/config.json",
	"formatter": {
		"biome": {
			"command": ["bash", "-c", "cd ui && biome format --write $FILE"]
		},
		"ruff": {
			"command": ["uvx", "ruff", "format", "$FILE"]
		}
	}
}
